# Chat App Example

Interactive chat application using the Claude Agent SDK.

## Features

- Multi-turn conversation support
- Streaming responses
- Session management

## Usage

```bash
go run main.go
```

Enter messages at the prompt. Type `quit` to exit.
